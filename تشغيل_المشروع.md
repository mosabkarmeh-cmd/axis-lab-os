# AXIS LAB — Complete Windows-ready copy (all fixes applied)

## ⚡ Easiest way (one-click installer)
Double-click **`install.bat`**.

It will automatically:
- Check that Node.js is installed (and tell you exactly where to download it if not)
- Try to install PostgreSQL for you (via `winget`) if it isn't already on your computer
- Install all the program's dependencies
- Create the database tables it needs
- Tell you clearly what's missing if anything fails, instead of a confusing error

Once it finishes, double-click **`start.bat`** to run the program, then open your
browser at `http://localhost:3000`.

Tested end-to-end (fresh machine, no PostgreSQL, no dependencies) - works.

---

## Manual method (same result, if you want to control each step yourself)

هاي كل الكود مصلّح ومجهّز 100%. الشي الوحيد يلي **ما بقدر** أرسلو جواها هو مجلد
`node_modules` (مكتبات جافاسكربت الجاهزة)، لسببين:
1. حجمه لحاله 585 ميغا — أكبر من كل باقي المشروع بأضعاف.
2. فيه ملفات "binary" مبنية خصيصاً لنظام التشغيل يلي جربت عليه الكود (Linux)، وإذا
   جهازك Windows أو Mac ما رح تشتغل عندك أصلاً — لازم تنبني من جديد عندك بأي حال.

بكل باقي الملفات (السيرفر، الفرونت إند، الإعدادات، حتى ملف `.env` معبّى بقيم تشتغل
مباشرة) — ما ناقص شي غير خطوتين تلقائيتين بيعملهم أي مشروع Node.js:

## التشغيل (3 أوامر بس، ولا أي تعديل يدوي مطلوب لو عندك Postgres محلي)

افتح تيرمينال جوا مجلد المشروع بعد فك الضغط، ونفّذ بالترتيب:

```bash
# 1. تثبيت المكتبات (هاي الخطوة يلي محل node_modules)
npm install

# 2. بناء جداول قاعدة البيانات (يشمل كل الجداول/الأعمدة الجديدة يلي أضفناها)
npx drizzle-kit push --config=src/db/drizzle.config.ts

# 3. تشغيل السيرفر
npm run dev
```

بعدها افتح المتصفح على `http://localhost:3000`.

## شرط واحد بس: قاعدة بيانات Postgres شغالة

ملف `.env` المرفق معبّى مسبقاً بـ:
```
SQL_HOST=localhost
SQL_USER=postgres
SQL_PASSWORD=axislab_dev_pass
SQL_DB_NAME=axislab
```

- **إذا ما عندك Postgres مركّب أصلاً**: نزّل PostgreSQL (نسخة 16 أو أحدث) من
  postgresql.org، ركّبه، بعدين أنشئ مستخدم وقاعدة بيانات بنفس هالأسماء تماماً (أو
  عدّل `.env` بأي أسماء تفضّلها).
- **إذا عندك Postgres أصلاً بإعداد مختلف**: افتح `.env` وعدّل القيم الأربعة بس
  (المضيف، المستخدم، الباسورد، اسم القاعدة).

## حسابات الدخول الافتراضية
- admin@axislab.com / admin123 (مدير — كل الصلاحيات)
- employee@axislab.com / employee123 (موظف)
- accountant@axislab.com / accountant123 (محاسب)

## لو بدك ميزات الذكاء الاصطناعي (Gemini)
افتح `.env` وحط مفتاح API تبعك بدل `MY_GEMINI_API_KEY`. بدون هيك، البرنامج بيشتغل
عادي، بس ميزات الذكاء الاصطناعي (اقتراح الأسعار، التدريب، إلخ) رح تكون معطلة.

## ملخص الإصلاحات
راجع `README_التعديلات.md` بنفس المجلد لتفاصيل كل شي انصلح هاي الجلسة.
