# 📋 AXIS LAB — برومت تفصيلي للإضافات المقترحة (Notifier, Print, Import, etc.)

**هذا برومت تفصيلي للإضافات التي اقترحتها سابقاً. يمكنك استخدامه لشرح كل إضافة على حدة، أو إرساله كاملًا لأي مطور، أو كمرجع لتوثيق هذه الميزات.**

---

## 🧩 الإضافات المقترحة

| # | الإضافة | الأولوية |
|---|---------|----------|
| 1 | نظام الإشعارات والتنبيهات | 🔴 أولوية قصوى |
| 2 | طباعة المستندات (PDF) | 🔴 أولوية قصوى |
| 3 | استيراد البيانات (Bulk Import) | 🔴 أولوية قصوى |
| 4 | البحث العام (Global Search) | 🟡 أولوية عالية |
| 5 | سلة المحذوفات (Recycle Bin) | 🟡 أولوية عالية |
| 6 | حالات الطلب المخصصة | 🟡 أولوية عالية |
| 7 | إعدادات الترقيم | 🟡 أولوية عالية |
| 8 | لوحة تحكم متقدمة (Charts) | 🟢 مستقبلية |

---

## 🔴 1. نظام الإشعارات والتنبيهات (Notification System)

### الهدف
إبقاء المستخدمين على اطلاع دائم بالأحداث المهمة داخل النظام، مثل:
- **المخزون المنخفض**: مادة أصبحت أقل من الحد الأدنى.
- **طلبات متأخرة**: طلب تجاوز تاريخ التسليم المحدد.
- **إنتاج مكتمل**: Job أصبح جاهزاً.
- **دفعة جديدة**: تم تسجيل دفعة مالية جديدة.
- **مستخدم جديد**: تم إنشاء حساب مستخدم جديد.

### المكونات

#### 1.1. Backend (الخادم)
- **جدول قاعدة البيانات**: `notifications` (معرف، المستخدم، العنوان، الرسالة، النوع، الحالة، تاريخ الإنشاء).
- **دوال الخدمة**:
  - `createNotification(userId, title, message, type)`
  - `getNotifications(userId, limit)`
  - `markAsRead(notificationId)`
  - `markAllAsRead(userId)`
- **مشغلات تلقائية**:
  - عند إضافة مادة جديدة: التحقق من المخزون.
  - عند تغيير حالة الطلب: التحقق من التاريخ.
  - عند اكتمال Job: إرسال إشعار.
  - عند تسجيل دفعة: إرسال إشعار للمحاسب والمدير.

#### 1.2. Frontend (الواجهة)
- **أيقونة الجرس**: تعرض عدد الإشعارات غير المقروءة.
- **قائمة منسدلة**: تعرض آخر 10 إشعارات مع زر "عرض الكل".
- **شاشة الإشعارات**: صفحة منفصلة تعرض جميع الإشعارات مع فلترة حسب النوع والتاريخ.
- **إشعارات فورية (Toasts)**: تظهر في الزاوية العلوية اليمنى عند حدوث حدث جديد.

#### 1.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| جلب الإشعارات | GET | `/api/notifications` |
| تحديد إشعار كمقروء | PATCH | `/api/notifications/:id/read` |
| تحديد الكل كمقروء | PATCH | `/api/notifications/read-all` |
| إنشاء إشعار يدوي | POST | `/api/notifications` |

---

## 🔴 2. طباعة المستندات (Printing System)

### الهدف
طباعة المستندات الرسمية للتعامل مع العملاء والموردين، مثل:
- **الفواتير (Invoices)**.
- **أوامر الإنتاج (Work Orders)**.
- **سندات التسليم (Delivery Notes)**.
- **عروض الأسعار (Quotations)**.

### المكونات

#### 2.1. Backend (الخادم)
- **محرك الطباعة**: استخدام `PDFKit` أو `Puppeteer` لتوليد ملفات PDF.
- **قوالب PDF**: قوالب جاهزة لكل نوع مستند (فاتورة، أمر إنتاج، إلخ).
- **دوال الخدمة**:
  - `generateInvoicePDF(orderId)` → ترجع مسار الملف PDF.
  - `generateWorkOrderPDF(jobId)` → ترجع مسار الملف PDF.
  - `generateDeliveryNotePDF(orderId)` → ترجع مسار الملف PDF.
- **تصدير مباشر**: إمكانية تنزيل الملف PDF مباشرة من المتصفح أو حفظه في مجلد `exports/`.

#### 2.2. Frontend (الواجهة)
- **زر "طباعة"**: في صفحة الطلب، الفاتورة، أو Job.
- **معاينة قبل الطباعة**: عرض المستند في نافذة منبثقة مع خيار الطباعة أو تنزيل PDF.
- **اختصارات**: `Ctrl+P` لفتح الطباعة مباشرة.

#### 2.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| طباعة فاتورة | GET | `/api/print/invoice/:id` |
| طباعة أمر إنتاج | GET | `/api/print/work-order/:id` |
| طباعة سند تسليم | GET | `/api/print/delivery/:id` |
| طباعة عرض سعر | GET | `/api/print/quotation/:id` |

---

## 🔴 3. استيراد البيانات (Bulk Import)

### الهدف
إدخال كميات كبيرة من البيانات دفعة واحدة لتوفير الوقت عند إعداد النظام أو إضافة منتجات جديدة.

### المكونات

#### 3.1. Backend (الخادم)
- **نوع الملفات المدعومة**: CSV, Excel (XLSX).
- **مكتبة المعالجة**: استخدام `csv-parser` و `exceljs`.
- **التحقق من صحة البيانات**: التأكد من وجود الحقول المطلوبة، وتنسيق البيانات بشكل صحيح.
- **دوال الخدمة**:
  - `importCustomers(file)` → استيراد العملاء.
  - `importProducts(file)` → استيراد المنتجات.
  - `importMaterials(file)` → استيراد المواد.
- **تقرير الاستيراد**: عرض عدد السجلات المستوردة، وعدد السجلات المرفوضة مع أسباب الرفض.

#### 3.2. Frontend (الواجهة)
- **زر "استيراد"**: في شاشات العملاء، المنتجات، المواد.
- **نافذة رفع ملف**: اختيار ملف CSV أو Excel.
- **معاينة البيانات**: عرض أول 5 صفوف من الملف قبل الاستيراد الفعلي.
- **تقرير الاستيراد**: عرض نافذة بنتيجة الاستيراد (نجاح/فشل).

#### 3.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| استيراد العملاء | POST | `/api/import/customers` |
| استيراد المنتجات | POST | `/api/import/products` |
| استيراد المواد | POST | `/api/import/materials` |

---

## 🟡 4. البحث العام (Global Search)

### الهدف
الوصول السريع إلى أي كيان في النظام (طلب، عميل، منتج، مادة) من مكان واحد.

### المكونات

#### 4.1. Backend (الخادم)
- **دوال البحث**:
  - `searchAll(query)` → تبحث في: الطلبات، العملاء، المنتجات، المواد، الفواتير، الدفعات.
  - إرجاع النتائج مع تصنيفها حسب النوع (Order, Customer, Product, Material, Invoice, Payment).
- **تحسين الأداء**: استخدام `LIKE` مع `ILIKE` في PostgreSQL، وإضافة `Indexes` على الحقول المستخدمة في البحث.

#### 4.2. Frontend (الواجهة)
- **شريط بحث في أعلى الصفحة**: ثابت في كل الشاشات.
- **اختصار لوحة المفاتيح**: `Ctrl+K` لفتح شريط البحث.
- **نتائج البحث**: قائمة منسدلة تعرض النتائج مرتبة حسب النوع مع أيقونات مميزة.
- **الانتقال المباشر**: النقر على نتيجة يفتح الكيان مباشرة (طلب، عميل، منتج، إلخ).

#### 4.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| البحث العام | GET | `/api/search?q=query` |

---

## 🟡 5. سلة المحذوفات (Recycle Bin)

### الهدف
استعادة العناصر المحذوفة (الطلبات، العملاء، المنتجات، المواد) في حال تم حذفها عن طريق الخطأ.

### المكونات

#### 5.1. Backend (الخادم)
- **إضافة حقل**: `deletedAt` في جميع الجداول الرئيسية (موجود بالفعل).
- **دوال الخدمة**:
  - `getDeletedOrders()` → جلب الطلبات المحذوفة.
  - `restoreOrder(id)` → استعادة طلب محذوف.
  - `permanentlyDeleteOrder(id)` → حذف نهائي للطلب.
- **تنظيف تلقائي**: حذف نهائي للعناصر المحذوفة منذ أكثر من 30 يوماً (مهمة خلفية).

#### 5.2. Frontend (الواجهة)
- **صفحة "سلة المحذوفات"**: تعرض جميع العناصر المحذوفة (طلبات، عملاء، منتجات، مواد).
- **زر "استعادة"**: استعادة العنصر إلى حالته الأصلية.
- **زر "حذف نهائي"**: حذف العنصر نهائياً (مع تأكيد).
- **فلترة**: تصفية حسب النوع (Orders, Customers, Products, Materials) والتاريخ.

#### 5.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| جلب المحذوفات | GET | `/api/recycle-bin` |
| استعادة | POST | `/api/recycle-bin/restore/:id` |
| حذف نهائي | DELETE | `/api/recycle-bin/permanent/:id` |

---

## 🟡 6. حالات الطلب المخصصة (Custom Order Status)

### الهدف
مرونة في إدارة دورة حياة الطلب بحيث يمكن للمدير إضافة أو تعديل حالات الطلب حسب احتياج الورشة.

### المكونات

#### 6.1. Backend (الخادم)
- **جدول** `order_statuses`: (id, name, color, order, isDefault).
- **دوال الخدمة**:
  - `getStatuses()` → جلب جميع الحالات.
  - `createStatus(name, color, order)` → إضافة حالة جديدة.
  - `updateStatus(id, name, color, order)` → تعديل حالة.
  - `deleteStatus(id)` → حذف حالة (إذا لم تستخدم في أي طلب).
- **التحقق**: عدم السماح بحذف حالة مستخدمة.

#### 6.2. Frontend (الواجهة)
- **شاشة إدارة الحالات**: في الإعدادات → حالات الطلب.
- **عرض قائمة الحالات**: جدول يعرض الاسم، اللون، الترتيب.
- **إضافة حالة جديدة**: نموذج مع حقول: الاسم، اللون.
- **سحب وإفلات**: ترتيب الحالات يدوياً.

#### 6.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| جلب الحالات | GET | `/api/order-statuses` |
| إضافة حالة | POST | `/api/order-statuses` |
| تعديل حالة | PUT | `/api/order-statuses/:id` |
| حذف حالة | DELETE | `/api/order-statuses/:id` |

---

## 🟡 7. إعدادات الترقيم (Numbering Format)

### الهدف
تخصيص نمط ترقيم المستندات (الطلبات، الفواتير، أوامر الإنتاج) ليتناسب مع نظام الورشة.

### المكونات

#### 7.1. Backend (الخادم)
- **جدول** `numbering_settings`: (id, entity, prefix, suffix, digits, nextNumber, separator).
- **دوال الخدمة**:
  - `getNumberingSettings(entity)` → جلب إعدادات ترقيم لكيان معين.
  - `updateNumberingSettings(entity, settings)` → تحديث الإعدادات.
  - `generateNextNumber(entity)` → توليد الرقم التالي بناءً على الإعدادات.
- **التنسيق**: `{prefix}{separator}{number}{suffix}` (مثال: `ORD-2025-0001`).

#### 7.2. Frontend (الواجهة)
- **شاشة إعدادات الترقيم**: في الإعدادات → الترقيم.
- **لكل كيان**: طلبات، فواتير، أوامر إنتاج، عروض أسعار، دفعات.
- **خيارات التخصيص**: بادئة (Prefix)، لاحقة (Suffix)، عدد الأرقام (Digits)، الفاصل (Separator)، الرقم التالي (Next Number).

#### 7.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| جلب الإعدادات | GET | `/api/numbering/:entity` |
| تحديث الإعدادات | PUT | `/api/numbering/:entity` |

---

## 🟢 8. لوحة تحكم متقدمة (Charts & Analytics)

### الهدف
عرض رسوم بيانية وتفاعلية للإيرادات، الأرباح، الطلبات، والإنتاج لاتخاذ قرارات أفضل.

### المكونات

#### 8.1. Backend (الخادم)
- **دوال الخدمة**:
  - `getRevenueChart(period)` → بيانات الإيرادات اليومية/الشهرية/السنوية.
  - `getProfitChart(period)` → بيانات الأرباح مع المصروفات.
  - `getOrdersChart(period)` → عدد الطلبات حسب الحالة.
  - `getProductionChart(period)` → عدد Jobs حسب الحالة.

#### 8.2. Frontend (الواجهة)
- **مكتبة الرسوم البيانية**: `Chart.js` أو `Recharts`.
- **أنواع الرسوم**: خطية (Line)، شريطية (Bar)، دائرية (Pie).
- **فلترة الفترة**: اليوم، الأسبوع، الشهر، السنة، مخصصة.
- **تفاعلية**: التمرير على الرسم البياني لعرض التفاصيل.

#### 8.3. واجهة برمجة (API)
| الإجراء | الطريقة | المسار |
|---------|---------|--------|
| بيانات الإيرادات | GET | `/api/charts/revenue` |
| بيانات الأرباح | GET | `/api/charts/profit` |
| بيانات الطلبات | GET | `/api/charts/orders` |
| بيانات الإنتاج | GET | `/api/charts/production` |

---

## ✅ ملخص الإضافات المقترحة

| # | الإضافة | الأولوية | المسار التقريبي |
|---|---------|----------|-----------------|
| 1 | الإشعارات | 🔴 قصوى | `api/notifications` |
| 2 | الطباعة | 🔴 قصوى | `api/print` |
| 3 | الاستيراد | 🔴 قصوى | `api/import` |
| 4 | البحث العام | 🟡 عالية | `api/search` |
| 5 | سلة المحذوفات | 🟡 عالية | `api/recycle-bin` |
| 6 | حالات الطلب المخصصة | 🟡 عالية | `api/order-statuses` |
| 7 | إعدادات الترقيم | 🟡 عالية | `api/numbering` |
| 8 | لوحة تحكم متقدمة | 🟢 مستقبلية | `api/charts` |

---

## 📦 هيكل المجلدات المقترح للإضافات

```
src/main/
├── services/
│   ├── notification.service.ts
│   ├── print.service.ts
│   ├── import.service.ts
│   ├── search.service.ts
│   ├── recycle-bin.service.ts
│   ├── order-status.service.ts
│   ├── numbering.service.ts
│   └── chart.service.ts
├── controllers/
│   ├── notification.controller.ts
│   ├── print.controller.ts
│   ├── import.controller.ts
│   ├── search.controller.ts
│   ├── recycle-bin.controller.ts
│   ├── order-status.controller.ts
│   ├── numbering.controller.ts
│   └── chart.controller.ts
└── routes/
    ├── notification.routes.ts
    ├── print.routes.ts
    ├── import.routes.ts
    ├── search.routes.ts
    ├── recycle-bin.routes.ts
    ├── order-status.routes.ts
    ├── numbering.routes.ts
    └── chart.routes.ts
```

---

**هذا البرومت التفصيلي يغطي جميع الإضافات التي اقترحتها سابقاً. يمكنك استخدامه لتوزيع المهام على فريق التطوير، أو لشرح الميزات لأي مطور جديد.** 🚀
